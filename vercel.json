{
    "version": 2,
    "builds": [{ "src": "bin/www", "use": "@vercel/node" }],
    "routes": [
        { "src": "/(.*)", "dest": "bin/www" },
        { "src": "/(.*)", "dest": "app.js" }
    ],
    "env": {
      "NODE_VERSION": "18.x"
    }
  }