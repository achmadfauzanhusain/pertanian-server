{
    "version": 2,
    "builds": [{ "src": "bin/www", "use": "@vercel/node" }],
    "routes": [
        { "src": "/(.*)", "dest": "bin/www" }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/bin/www" }],
    "env": {
      "NODE_VERSION": "18.x"
    }
  }